name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18

    - name: Install dependencies
      working-directory: ./Codigo
      run: npm install
      
   # - name: Run Cucumber tests
   #   working-directory: Codigo
   #   run: npm run test:cucumber

   # - name: Run Mocha tests
   #   working-directory: Codigo
   #   run: npm run test:mocha


    - name: Debug Environment Variables
      env:
        SUPABASE_URL: 'https://gltustnhaqwwjigoerrx.supabase.co'
        SUPABASE_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdsdHVzdG5oYXF3d2ppZ29lcnJ4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3MzI4MzQ0MDMsImV4cCI6MjA0ODQxMDQwM30.O_HEJ9CTo5GYX06xj0OEBxvWeT1lYlKuP2ThnezvKCM'
      run: |
        echo "SUPABASE_URL=${{ env.SUPABASE_URL }}"
        [ -z "${{ env.SUPABASE_KEY }}" ] && echo "SUPABASE_KEY is missing" || echo "SUPABASE_KEY is set"

    - name: Run Application
      working-directory: Codigo
      env:
        SUPABASE_URL: 'https://gltustnhaqwwjigoerrx.supabase.co'
        SUPABASE_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdsdHVzdG5oYXF3d2ppZ29lcnJ4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3MzI4MzQ0MDMsImV4cCI6MjA0ODQxMDQwM30.O_HEJ9CTo5GYX06xj0OEBxvWeT1lYlKuP2ThnezvKCM'
      run: npm start

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    
    steps:
     - name: Deploy to Docker
       working-directory: Codigo
       run: |
        docker build -t acttus-cosmeticos .
        docker run -d -p 3000:3000 acttus-cosmeticos
